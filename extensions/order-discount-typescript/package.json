{
  "name": "order-discount-typescript",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "npm exec -- shopify",
    "typegen": "npm exec -- shopify app function typegen",
    "build": "npm exec -- shopify app function build",
    "preview": "npm exec -- shopify app function run",
    "function-build": "npm exec -- shopify app function build",
    "function-preview": "cat ../../inputs/input.json | npm exec -- shopify app function run",
    "function-validate": "cat ../../inputs/input.json | node ../../validate-function-output.js"
  },
  "codegen": {
    "schema": "schema.graphql",
    "documents": "input.graphql",
    "generates": {
      "./generated/api.ts": {
        "plugins": [
          "typescript",
          "typescript-operations"
        ]
      }
    }
  }
}
